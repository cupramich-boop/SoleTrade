{
  "installCommand": "echo 'skip — flutter pub get runs in buildCommand'",
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable _flutter_sdk && export PATH=\"$PATH:$(pwd)/_flutter_sdk/bin\" && flutter config --enable-web && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web"
}
